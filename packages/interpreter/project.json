{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "root": "packages/interpreter",
  "targets": {
    "build": {
      "command": "tsc -p packages/interpreter/tsconfig.json",
      "inputs": ["{workspaceRoot}/tsconfig.json"]
    }
  }
}
